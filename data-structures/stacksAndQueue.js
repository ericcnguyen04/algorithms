/* 
Distinguish between the behavior of a stack and a queue.
Determine situations in which you’d use a stack or queue versus another data structure.
Build a stack and a queue using a linked list or an array.
*/

const stacks = [
    'most recently added item, is the first to be removed'
]

const queues =[
    'first-in, first-out',
    'priority queue: contains a priority queue',
    'double-end queue: performs at both ends'
]

const stacksAndQueues = [
    'both can be implemented as an array or linked list'
]

